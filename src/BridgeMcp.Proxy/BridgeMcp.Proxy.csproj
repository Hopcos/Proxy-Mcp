<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <AssemblyName>bridgemcp</AssemblyName>
    <RootNamespace>BridgeMcp</RootNamespace>
    <Description>BridgeMcp — a proxy that exposes STDIO Model Context Protocol servers over the Streamable HTTP transport.</Description>
  </PropertyGroup>

</Project>
